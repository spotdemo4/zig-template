#:schema https://www.schemastore.org/treefmt.json

[formatter.zig]
command = "zig"
options = ["fmt"]
includes = ["*.zig"]

[formatter.nixfmt]
command = "nixfmt"
includes = ["*.nix"]

[formatter.oxfmt]
command = "oxfmt"
options = ["--write"]
includes = ["*.json", "*.yaml", "*.toml", "*.md"]
